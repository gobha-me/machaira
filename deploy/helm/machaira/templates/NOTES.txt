Machaira is available at:
{{- if .Values.ingress.enabled }}
  {{ ternary "https" "http" .Values.ingress.tls.enabled }}://{{ .Values.ingress.host }}
{{- else }}
  kubectl port-forward --namespace {{ .Release.Namespace }} service/{{ include "machaira.fullname" . }} 5274:{{ .Values.service.port }}
  http://127.0.0.1:5274
{{- end }}

The application will offer first-administrator setup while its database is empty.
{{- if .Values.stt.sidecar.enabled }}

Private STT is enabled with {{ .Values.stt.sidecar.model }}. The model is downloaded into the
persistent stt-model-cache directory before the sidecar becomes ready. In Machaira, select the
Speaches preset under Settings -> Voice input; the in-Pod base URL is http://127.0.0.1:8000/v1.
{{- end }}
