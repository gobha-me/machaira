apiVersion: v1
kind: Service
metadata:
  name: {{ include "machaira.fullname" . }}
  labels:
    {{- include "machaira.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "machaira.selectorLabels" . | nindent 4 }}
