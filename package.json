{
  "name": "machaira",
  "version": "0.19.0",
  "private": true,
  "description": "Sword — a SWORD-family Bible study tool (Vue 3 + Vite frontend, Node SWORD backend)",
  "license": "GPL-2.0-or-later",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm -w shared run build && concurrently -n server,client -c cyan,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "build": "npm -w shared run build && npm -w server run build && npm -w client run build",
    "audit:catalog": "node scripts/audit-catalog.mjs",
    "postinstall": "patch-package && npm rebuild node-sword-interface"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "patch-package": "^8.0.1"
  }
}
